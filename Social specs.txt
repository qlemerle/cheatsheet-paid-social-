<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cheatsheet Paid Social - Spécifications Techniques</title>

    <style>
        :root {
            --primary-color: #2563eb;
            --bg-color: #f8fafc;
            --text-color: #1e293b;
            --border-color: #e2e8f0;
        }

        body {
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            margin: 0;
            padding: 40px 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 10px;
            color: #0f172a;
        }

        .subtitle {
            text-align: center;
            margin-bottom: 30px;
            color: #64748b;
        }

        /* Tabs */
        .tab {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            background-color: #fff;
            border: 1px solid var(--border-color);
            border-radius: 8px 8px 0 0;
            max-width: 1200px;
            margin: 0 auto;
        }

        .tab button {
            background: none;
            border: none;
            cursor: pointer;
            padding: 14px 24px;
            font-size: 16px;
            font-weight: 600;
            color: #475569;
            border-bottom: 3px solid transparent;
        }

        .tab button:hover {
            background-color: #f1f5f9;
        }

        .tab button.active {
            color: var(--primary-color);
            border-bottom: 3px solid var(--primary-color);
        }

        /* Content */
        .tabcontent {
            display: none;
            padding: 30px;
            background-color: #fff;
            border: 1px solid var(--border-color);
            border-top: none;
            border-radius: 0 0 8px 8px;
            max-width: 1200px;
            margin: 0 auto;
            box-shadow: 0 4px 6px rgba(0,0,0,0.08);
            animation: fadeEffect 0.4s;
        }

        @keyframes fadeEffect {
            from {opacity: 0;}
            to {opacity: 1;}
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 12px 15px;
            border-bottom: 1px solid var(--border-color);
        }

        th {
            background: #f8fafc;
            font-weight: 600;
        }

        tr:hover {
            background: #f1f5f9;
        }

        .expert-tip {
            background: #eff6ff;
            border-left: 4px solid var(--primary-color);
            padding: 15px;
            margin-bottom: 20px;
            font-size: 0.95em;
        }
    </style>
</head>

<body>

<h1>CheatSheet Media Buyer 2026</h1>
<div class="subtitle">Spécifications techniques par plateforme sociale</div>

<div class="tab">
    <button class="tablinks" onclick="openPlatform(event, 'Meta')" id="defaultOpen">META</button>
    <button class="tablinks" onclick="openPlatform(event, 'TikTok')">TikTok</button>
    <button class="tablinks" onclick="openPlatform(event, 'Snap')">Snapchat</button>
    <button class="tablinks" onclick="openPlatform(event, 'Pinterest')">Pinterest</button>
    <button class="tablinks" onclick="openPlatform(event, 'LinkedIn')">LinkedIn</button>
    <button class="tablinks" onclick="openPlatform(event, 'Reddit')">Reddit</button>
    <button class="tablinks" onclick="openPlatform(event, 'X')">X</button>
    <button class="tablinks" onclick="openPlatform(event, 'BeReal')">BeReal</button>
</div>

<!-- META -->
<div id="Meta" class="tabcontent">
    <div class="expert-tip">
        <strong>Tips de l'expert :</strong> Mobile First. Le 4:5 prend plus de place dans le feed.
    </div>
    <!-- tableau Meta conservé à l’identique -->
</div>

<!-- Autres plateformes : contenu inchangé -->
<!-- (TikTok, Snap, Pinterest, LinkedIn, Reddit, X, BeReal) -->

<script>
    function openPlatform(evt, platformName) {
        let tabcontent = document.getElementsByClassName("tabcontent");
        for (let i = 0; i < tabcontent.length; i++) {
            tabcontent[i].style.display = "none";
        }

        let tablinks = document.getElementsByClassName("tablinks");
        for (let i = 0; i < tablinks.length; i++) {
            tablinks[i].classList.remove("active");
        }

        document.getElementById(platformName).style.display = "block";
        evt.currentTarget.classList.add("active");
    }

    document.getElementById("defaultOpen").click();
</script>

</body>
</html>
